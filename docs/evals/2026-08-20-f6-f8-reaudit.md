# F Re-Audit Report — 2026-08-20 (Sesi 21)

## F6. `js/pages/master_full.js` — Master Full Form (803 baris)

### Temuan:

| #   | Finding                                                                                | Severity | Fix                                 |
| --- | -------------------------------------------------------------------------------------- | -------- | ----------------------------------- |
| 1   | **Hardcoded "File Tersimpan"** — photoInfo/jftInfo/sswInfo innerHTML                   | LOW      | ✅ `tr('form.mf_file_saved')`       |
| 2   | **Missing DOM null check** — `document.getElementById('wa-display')` etc (IIFE onload) | LOW      | ✅ Guard with `if (el)`             |
| 3   | `alert()` in changeStep() catch block                                                  | LOW      | ⏭ Skip (error boundary, acceptable) |

### Not checked (OK):

- `appendHTML()`: XSS-safe via `escapeHtml()` ✅
- `submitMaster()`: all field reads use `valSafe()` (null-safe) ✅
- Dropdowns: domain data (SSW/PEKERJAAN), bilingual labels ✅

---

## F7. `js/pages/share.js` — Share Page (581 baris)

### Already fixed in previous session (Sesi 21):

- XSS: `safeName` now escapes `\`, `'`, `"` ✅
- `renderGrid()`: uses `escapeHtml()` for onclick ✅

---

## F8. `js/pages/siswa_baru.js` — Siswa Baru (503 baris)

### Temuan:

| #   | Finding                                                                                    | Severity | Fix                              |
| --- | ------------------------------------------------------------------------------------------ | -------- | -------------------------------- |
| 1   | **XSS: `hasil.name` in innerHTML** — `handleDocUpload()` renders filename without escaping | **HIGH** | ✅ `escapeHtml(hasil.name)`      |
| 2   | **10+ hardcoded alert() strings** — welcome, error, success messages                       | MEDIUM   | ✅ → `showToast()` + `tr()` keys |
| 3   | **3 hardcoded button labels** — "MENGIRIM…", "BERHASIL!", "SUBMIT DATA"                    | MEDIUM   | ✅ `tr()` keys                   |
| 4   | **Missing null check** — `$('aiTypingStatus').innerHTML` without guard                     | LOW      | ✅ Null guard                    |
