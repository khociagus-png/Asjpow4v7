# Debug Audit: B3. `js/init/nav.js` — Navigation & Logout

**Tanggal:** 2026-08-20
**Pengerja:** Buffy (Codebuff)
**File:** `js/init/nav.js` (156 baris)
**Status:** ✅ Diverifikasi — 0 critical, 1 low (redundant code)

---

## 1. Exports (4)

| Export               | Purpose                           |
| -------------------- | --------------------------------- |
| `changePage(page)`   | Switch public/admin/kandidat page |
| `closeMobileMenu()`  | Close mobile nav drawer           |
| `toggleMobileMenu()` | Toggle mobile nav open/close      |
| `logoutApp()`        | Full logout flow                  |

---

## 2. Logout Flow Analysis

### ✅ Cleanup Order (correct)

1. **Timer cleanup**: `clearInterval(AUTO_REFRESH_TIMER)` + set null
2. **Server logout**: `callAPI('logout')` best-effort (non-blocking)
3. **localStorage cleanup**: 10 keys removed (session, auth, refresh)
4. **Nav reset**: admin/kandidat nav hidden, logged-out shown
5. **State reset**: `isAdmin=false`, `isKandidat=false`, names cleared
6. **Mobile nav**: `closeMobileMenu()`
7. **Page switch**: `changePage('public')`

### ✅ localStorage Keys Removed (10)

```
asj_admin_login, asj_admin_name, asj_admin_session, asj_admin_refresh,
asj_kandidat_login, asj_kandidat_name, asj_kandidat_wa, asj_kandidat_session,
asj_kandidat_refresh, asj_session_token
```

- Theme preference (`asj_lang`, `asj_theme`) NOT removed → user settings preserved ✅
- Draft CV NOT removed → user work preserved ✅

### ✅ Server Logout

```javascript
window.callAPI('logout', []).catch(function () {});
```

- Best-effort: tidak menunda logout ✅
- `.catch()` → silent failure (server down ≠ user stuck) ✅

---

## 3. Low Priority Finding

### ⚠️ LOW: Double clearInterval

```javascript
// Line 85 — using window.AUTO_REFRESH_TIMER (accessor)
if (window.AUTO_REFRESH_TIMER) {
  clearInterval(window.AUTO_REFRESH_TIMER);
  window.AUTO_REFRESH_TIMER = null;
}

// Line 138 — using AUTO_REFRESH_TIMER (imported from state.js)
if (AUTO_REFRESH_TIMER) {
  clearInterval(AUTO_REFRESH_TIMER);
  window.AUTO_REFRESH_TIMER = null;
  window.PREV_MAIL_COUNT = null;
}
```

- **Impact**: Redundant code, no functional impact (clearInterval twice = no-op)
- **Verdict**: LOW — cleanup could be cleaner but harmless

---

## 4. changePage() Analysis

### ✅ Correct

- Toggle `hidden` class on page containers ✅
- Re-render public filter when switching to public ✅
- Close mobile menu on page change ✅
- Toggle bottom nav (admin/kandidat) ✅
- Padding bottom for bottom nav ✅

---

## 5. Summary

| Item                 | Status | Notes                                                     |
| -------------------- | ------ | --------------------------------------------------------- |
| changePage           | ✅     | Toggle pages + re-render public                           |
| closeMobileMenu      | ✅     | CSS class toggle                                          |
| toggleMobileMenu     | ✅     | requestAnimationFrame for smooth animation                |
| logoutApp            | ✅     | Full cleanup: timer + server + localStorage + state + nav |
| Double clearInterval | ⚠️ LOW | Redundant, no impact                                      |
| Syntax               | ✅     | OK                                                        |

**Verdict: B3 DIVERIFIKASI — Logout flow solid. 1 low finding (redundant code).**
