# To Do List ASJ Portal — 2026-08-18 (terbaru)

> Status per 2026-08-18 malam (setelah sesi A+C+D). Live masih
> `app-0d473e8141`; lokal sudah `app-699dfb4a86` — paket fix di bawah **belum
> sampai ke domain asli** (butuh deploy — izin eksplisit pemilik).

## A. Segera — ✅ SELESAI

- [x] **Commit + push overhaul `scripts/check-handlers.mjs`** — commit `e90f53a`
      (termasuk header sesi PROGRESS2/CHANGELOG2 + todolist ini).

## B. Deploy ke Netlify (butuh izin eksplisit pemilik) — ⏳ MENUNGGU IZIN

Paket fix yang masih lokal saja (semua sudah terverifikasi di preview):

- [ ] **Fix `pwa.js` reload palsu halaman standalone** — ai_form/master-full dkk
      membandingkan cache-buster `?v=esm15` dengan hash bundel → selalu beda →
      purge+reload sekali tiap buka di live. Fix: hard-check hanya halaman bundel.
- [ ] **3 fix filter handler seam** — `filterKelolaLoker`, `filterCbx`
      (auto-search LOKASI/SYARAT di Tambah Job), `cariKandidatManual`
      (auto-fill kandidat manual) — ReferenceError tiap ketik di live.
- [ ] **Fix `cekRiwayat`** (apply-full) — radar "WA sudah pernah daftar"
      di form lamaran tidak pernah jalan.
- [ ] **Kartu Undangan Grup Kelas dipindah ke PUNCAK tab WA Pintar** + badge "Fitur Khusus" (supaya tidak terkesan hilang lagi).
- [ ] **Hardening `filterKandidat`** — `.toLowerCase()` tanpa guard (86/223
      kandidat punya tahapan kosong; NULL suatu saat = filter mati total).
- [ ] **Guard `check-handlers.mjs`** (overhaul: self-check cakupan event +
      parser seam presisi) — terpasang di `bun run build` + CI.
- [ ] **Guard runtime handler di browser** (`js/core/bridge.js`:
      `checkInlineHandlers` + `flushGuardWarnings`) — pelengkap scanner statis,
      tanpa daftar event, deferred-flush anti false-positive. Bundle lokal
      `app-699dfb4a86`.
- [ ] **E2E anti-race SW** — guard unregister SW di login-check,
      undang-grup-kelas, photo-check (upload/biodata sudah duluan).

## C. Env / Konfigurasi — ✅ SELESAI (diverifikasi via Netlify API 2026-08-18)

- [x] **`ADMIN_NUMBERS` sudah benar di Netlify**: `082130442661, 082229020129,
087864932711, 087728149733, 087889502004` (typo sudah diperbaiki).
- [x] **`ASJ_ADMINS` lengkap**: `SACHOU:1111,AYOK:2222,KHOLIS:3333,KHOCI:4444`.
- [x] `SESSION_SECRET` (64-hex) terisi; `ADMIN_MASTER_PIN=123456`; `PIN_KHOCI=4444`.
- [x] `CLOUDINARY_URL` tidak ada di Netlify — memang tidak dipakai kode
      (alur unsigned client-side, cloud `ybzzbw9i` hardcoded di js/cloudinary.js).

## D. Ide / Follow-up — ✅ D1+D2+D3 SELESAI, D4 opsional

- [x] **Guard runtime handler di browser (dev/preview)** — `bridge.js`
      `checkInlineHandlers()`/`flushGuardWarnings()`: scan atribut event APA PUN
      via `getAttributeNames` (tanpa daftar event), deferred-flush di load+3s
      (anti false positive modul yang register belakangan). Terverifikasi di
      preview: 0 false positive halaman sehat, menangkap handler rusak.
- [x] **Audit cakupan guard vs semua atribut event** — self-check di
      `check-handlers.mjs` sudah otomatis memaksa (event baru yang tidak
      didaftarkan = CI gagal); guard runtime menghapus blind spot sepenuhnya.
- [x] **Deploy otomatis** — build hook Netlify
      `6a84142ec210682c643028b8` + `.github/workflows/deploy-netlify.yml`
      (manual `workflow_dispatch`; auto-on-push tinggal buka komentar).
      DEPLOY.md §2.5 didokumentasikan. **Belum ada deploy yang dipicu.**
- [ ] **(Opsional) pindahkan build hook URL ke GitHub secret** — kalau mau
      lebih ketat dari URL di workflow.

## Sudah beres (sesi sebelumnya, verifikasi live)

- Live check menyeluruh: E2E share/login/undang-grup-kelas/photo/upload/
  biodata + responsif 390/768/1280 + Cloudinary preset `asjportal` ✓
- `ASJ_ADMINS` aktif (login SACHOU:1111 live) ✓
- 2 template WA (Undangan Grup Default + PEMBERITAHUAN GA LOLOS) ada di DB ✓
- Vitest 156/156, lint 0 error, build lulus ✓
