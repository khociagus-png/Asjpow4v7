# To Do List ASJ Portal — 2026-08-18 (terbaru)

> Status per 2026-08-18 malam. Kondisi nyata repo: tree bersih kecuali
> `scripts/check-handlers.mjs` (overhaul belum di-commit). Live masih
> `app-0d473e8141`; lokal `app-6080598722` — semua fix sejak sesi pwa.js
> **belum sampai ke domain asli** (butuh deploy + izin pemilik).

## A. Segera (siap eksekusi)

- [ ] **Commit + push overhaul `scripts/check-handlers.mjs`**
  - Isi header sesi di `PROGRESS2.md` / `CHANGELOG2.md` (tanggal + pengerja + hash)
  - Commit kode → commit dokumen → push `main`
  - Verifikasi pre-commit lulus (format + syntax)

## B. Deploy ke Netlify (butuh izin eksplisit pemilik)

Paket fix yang masih lokal saja (semua sudah terverifikasi di preview):

- [ ] **Fix `pwa.js` reload palsu halaman standalone** — ai_form/master-full dkk
      membandingkan cache-buster `?v=esm15` dengan hash bundel → selalu beda →
      purge+reload sekali tiap buka di live. Fix: hard-check hanya halaman bundel.
- [ ] **3 fix filter handler seam** — `filterKelolaLoker`, `filterCbx`
      (auto-search LOKASI/SYARAT di Tambah Job), `cariKandidatManual`
      (auto-fill kandidat manual) — ReferenceError tiap ketik di live.
- [ ] **Fix `cekRiwayat`** (apply-full) — radar "WA sudah pernah daftar"
      di form lamaran tidak pernah jalan.
- [ ] **Kartu Undangan Grup Kelas dipindah ke PUNCAK tab WA Pintar** + badge "Fitur Khusus" (supaya tidak terkesan hilang lagi).
- [ ] **Hardening `filterKandidat`** — `.toLowerCase()` tanpa guard (86/223
      kandidat punya tahapan kosong; NULL suatu saat = filter mati total).
- [ ] **Guard `check-handlers.mjs`** (overhaul: self-check cakupan event +
      parser seam presisi) — terpasang di `bun run build` + CI.
- [ ] **E2E anti-race SW** — guard unregister SW di login-check,
      undang-grup-kelas, photo-check (upload/biodata sudah duluan).

## C. Env / Konfigurasi

- [ ] **Netlify: fix typo `ADMIN_NUMBERS`** — `0082229020129` → `082229020129`
      (di `.env.local` sudah diperbaiki; pastikan Keys/API keys Netlify ikut).
- [ ] **Verifikasi `ASJ_ADMINS`** — SACHOU 1111 / AYOK 2222 / KHOLIS 3333 /
      KHOCI 4444 (login SACHOU:1111 sudah terbukti live; konfirmasi 4 nama
      lengkap terdaftar).
- [ ] **Opsional: rapikan `CLOUDINARY_URL`** — format placeholder
      `<key><secret>`; tidak dipakai kode (alur unsigned client-side),
      tidak berdampak fungsional.

## D. Ide / Follow-up (belum dikomitmen)

- [ ] **Guard runtime handler di browser (dev)** — bridge mengecek tiap
      `on*`/`data-action` benar-benar resolve ke `window`; menghapus blind
      spot daftar event statis tanpa butuh CI browser.
- [ ] **Audit cakupan guard vs semua atribut event** — onpaste, onwheel,
      onanimationend dkk (self-check sudah otomatis memaksa, audit = konfirmasi).
- [ ] **Deploy otomatis** — pipeline Netlify dari `main` (sekalian dengan
      kebijakan di DEPLOY.md).

## Sudah beres (sesi sebelumnya, verifikasi live)

- Live check menyeluruh: E2E share/login/undang-grup-kelas/photo/upload/
  biodata + responsif 390/768/1280 + Cloudinary preset `asjportal` ✓
- `ASJ_ADMINS` aktif (login SACHOU:1111 live) ✓
- 2 template WA (Undangan Grup Default + PEMBERITAHUAN GA LOLOS) ada di DB ✓
- Vitest 156/156, lint 0 error, build lulus ✓
